# ---------------------------------------------------------------------------
# API Environment Variables
# ---------------------------------------------------------------------------
# Copy this file to .env and adjust values as needed.
# Do NOT commit .env to version control.
# ---------------------------------------------------------------------------

# Server
PORT=3001

# Database (TBD — uncomment and configure when a DB is chosen)
# DB_PROVIDER=noop          # noop | postgres | sqlite | mongo (etc.)
# DATABASE_URL=              # Connection string for the chosen DB
