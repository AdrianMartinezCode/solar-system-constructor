Bootstrap the openspec/ structure for this project.

1. Read your role file: `.agents/roles/founder.md`
2. Read your core skill: `.agents/skills/openspec-init/SKILL.md`
3. Read the project config: `openspec/config.yaml`
4. Execute the Founder phase: initialize the `openspec/` directory structure and `config.yaml`.
5. Return a JSON result envelope with: status, executive_summary, artifacts, next_recommended, risks.
